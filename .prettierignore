node_modules
dist
build
coverage
test-results
playwright-report
pnpm-lock.yaml
docs/spec
.ultraplan
README.md

# Tauri-плагины перегенерируют это на каждом `cargo build`/`cargo check` в
# своём собственном, не-prettier формате (`tauri_plugin::Builder`, не наш
# код) — коммитить их прежним форматом бессмысленно, `format:check` будет
# заново краснеть после любой локальной пересборки (найдено при Task B3).
apps/mobile/src-tauri/plugins/*/permissions/autogenerated
apps/mobile/src-tauri/plugins/*/permissions/schemas

# Вендоренные ЧУЖИЕ крейты, подключённые через `[patch.crates-io]`
# (`tauri-plugin-notification` 2.4.0, ADR-0008, дополнение от 2026-09-04).
# Это дословная копия апстрима с одним точечным патчем: переформатировать её
# нашим prettier'ом значило бы зашумить diff против апстрима ровно там, где
# он обязан оставаться минимальным и обозримым при следующем обновлении.
apps/mobile/src-tauri/vendor
