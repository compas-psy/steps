{
  "name": "@shagi/importer",
  "version": "0.1.0",
  "private": true,
  "description": "Импорт Todoist/CSV и бэкапов ШАГОВ через CreateTaskCommand (SPEC §14) — без обращения к приватным API",
  "license": "UNLICENSED",
  "type": "module",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "lint": "oxlint ."
  },
  "dependencies": {
    "@js-temporal/polyfill": "^0.5.1",
    "@shagi/core": "workspace:*",
    "fflate": "0.8.3",
    "@shagi/storage": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "typescript": "5.9.3",
    "vitest": "^4.1.11"
  }
}
