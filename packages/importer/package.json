{
  "name": "@shagi/importer",
  "version": "0.1.0",
  "private": true,
  "description": "Импорт Todoist/CSV и бэкапов ШАГОВ через CreateTaskCommand (SPEC §14) — без обращения к приватным API",
  "license": "UNLICENSED",
  "type": "module",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "lint": "oxlint ."
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vitest": "^4.1.11"
  }
}
