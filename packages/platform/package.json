{
  "name": "@shagi/platform",
  "version": "0.1.0",
  "private": true,
  "description": "Интерфейсы возможностей платформы (SPEC §4: LocalDbPort, NotificationSchedulerPort и т.д.) — без реализаций, они живут в apps/*",
  "license": "UNLICENSED",
  "type": "module",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "lint": "oxlint ."
  },
  "dependencies": {
    "@js-temporal/polyfill": "^0.5.1"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vitest": "^4.1.11"
  }
}
