/**
 * `@shagi/telemetry` — санированные контракты событий телеметрии.
 *
 * Только ТИПЫ и схемы событий (что можно отправить и в какой форме) —
 * никакого кода сбора, батчинга или отправки, и никакого PII в схемах.
 * Redaction-тесты (`.ultraplan/open-questions.md` ?20) держат корпус
 * пользовательского контента и обязаны падать на любой утечке подстроки
 * в собранное событие — эти тесты приезжают вместе с реализацией сбора,
 * не в этом пакете работ.
 */
export const PACKAGE_NAME = '@shagi/telemetry' as const;
