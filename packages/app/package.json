{
  "name": "@shagi/app",
  "version": "0.1.0",
  "private": true,
  "description": "Все экраны, маршруты и поведение продукта (SPEC §3) — apps/* не содержат ни одного из них",
  "license": "UNLICENSED",
  "type": "module",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "lint": "oxlint ."
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vitest": "^4.1.11"
  }
}
