/**
 * `@shagi/storage` — репозитории и адаптеры персистентности: SQLite/WAL/FTS5
 * на нативе, IndexedDB (+ OPFS где доступен) в вебе (SPEC/00 §2).
 *
 * Обе платформы обязаны давать одинаковую observable search-семантику —
 * веб дублирует ранжирование FTS5, а не приближает его. React-компоненты
 * никогда не обращаются к SQLite/IndexedDB напрямую — только через
 * репозитории отсюда. Пакет не содержит доменных инвариантов: они
 * проверяются в `@shagi/core` до того, как мутация дойдёт до репозитория.
 *
 * Пакет работ E02.1 (эта волна) отдаёт контракт, схему, миграции,
 * tombstone-политику и эталонную реализацию в памяти — САМИ адаптеры
 * SQLite/IndexedDB это следующие пакеты работ (см. `docs/adr/0005-*`).
 * Общий набор тестов контракта — отдельный подпуть `@shagi/storage/contract`
 * (тянет `vitest`, поэтому не в этом файле — см. его собственный комментарий).
 */
export const PACKAGE_NAME = '@shagi/storage' as const;

// --- Контракт репозиториев и транзакции (п.2) --------------------------------
export * from './ports/index.js';

// --- Логическая схема: таблицы, индексы, FTS5 (п.3) ---------------------------
export * from './schema/index.js';

// --- Миграции: протокол безопасности + план базовой схемы (п.4) --------------
export * from './migration/index.js';

// --- Tombstone-политика (п.5) --------------------------------------------------
export * from './tombstone/index.js';

// --- Порт SQL-драйвера (ADR-0005) ----------------------------------------------
export * from './sqlite/index.js';

// --- Эталонная реализация в памяти (п.6) ---------------------------------------
export * from './memory/index.js';

// --- Служебные value-types пакета -----------------------------------------------
export { isNonEmptyArray, type NonEmptyArray } from './values.js';
