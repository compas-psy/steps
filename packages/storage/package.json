{
  "name": "@shagi/storage",
  "version": "0.1.0",
  "private": true,
  "description": "Репозитории и адаптеры персистентности: SQLite (native) / IndexedDB (web) — единая observable-семантика (SPEC §2)",
  "license": "UNLICENSED",
  "type": "module",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./contract": "./src/contract/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "lint": "oxlint ."
  },
  "dependencies": {
    "@js-temporal/polyfill": "^0.5.1",
    "@shagi/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "typescript": "5.9.3",
    "vitest": "^4.1.11"
  }
}
