{
  "name": "@shagi/core",
  "version": "0.1.0",
  "private": true,
  "description": "Домен ШАГОВ: сущности, команды, инварианты (SPEC §7.1), время (Temporal, §5), повторы, fractional rank (§6)",
  "license": "UNLICENSED",
  "type": "module",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "lint": "oxlint ."
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vitest": "^4.1.11"
  }
}
