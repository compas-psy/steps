//! ШАГИ — нативный мост к SQLite, общий для оболочек Android и Windows.
//!
//! Реализация — в `sqlite.rs`, а не здесь: `#[tauri::command]` в корне
//! крейта не компилируется (макрос генерирует `__cmd__*` дважды в одном
//! пространстве имён, `E0255`), поэтому команды обязаны лежать в модуле.
//! Проверено компилятором, не предположено.
//!
//! Потребители подключают команды как
//! `shagi_sqlite::sqlite::sqlite_open` и т.д.

pub mod sqlite;
